#include <iostream>
using namespace std;

template <typename T>
T Min(T a, T b) {
    return (a < b) ? a : b;
}

int main() {
    cout << getMin(10, 20) << endl;     
    cout << getMin(5.5, 2.3) << endl;  
    return 0;
}
