#include <iostream>
using namespace std;

template <typename T>
T Max(T a, T b) {
    return (a > b) ? a : b;
}

int main() {
    cout << Max(10, 20) << endl;          
    cout << Max(5.5, 2.3) << endl;         
    return 0;
}
